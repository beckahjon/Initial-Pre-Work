//I learned you can use === to return the result of the comparison instead of using an if/else statement.
function isLess(a, b) {
  // Fix this code
  return (a < b)

}

// Change these values to test
isLess(13, 26);
