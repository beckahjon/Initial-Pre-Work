//You can use the Math.random function to generate a random decimal number between 0 and 1. You can use a while loop with the Math.random function to generate random numbers while your variable result is a specified value.
function randomFraction() {

  // Only change code below this line.
    var result = 0;
   while (result === 0) {
       result = Math.random()
   }
   return result;
  // Only change code above this line.
}
