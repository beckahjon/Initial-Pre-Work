//If you need to use a literal quote inside your string, you can use a backslash in front of it to escape JavaScript from considering it the end of the string.
var myStr = "I am a \"double quoted\" string inside \"double quotes\"."; // Change this line
