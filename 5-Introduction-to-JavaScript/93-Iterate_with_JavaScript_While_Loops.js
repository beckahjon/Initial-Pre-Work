//A loop allows you to run the same code multiple times. A while loop runs while a condition is true and stops when the condition is no longer true.
// Setup
var myArray = [];

// Only change code below this line.
var i = 0;
while(i < 5) {
    myArray.push(i)
    i++;
}
