//I learned you can also generate random whole numbers by using Math.floor() to round down to the nearest whole number after you have gerenated a random decimal.
var randomNumberBetween0and19 = Math.floor(Math.random() * 20);

function randomWholeNum() {

  // Only change code below this line.
var result = 0;
while (result === 0) {
  return Math.floor(Math.random()*10)
}
  return Math.random();
}
