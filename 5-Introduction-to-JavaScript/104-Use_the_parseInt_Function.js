//There is a function called parseInt() that parses a string and returns an integer. If the first character of a string can't be converted to a number, it returns NaN.
function convertToInteger(str) {
var a = parseInt (str)
return a;
}

convertToInteger("Hamlet");
